/**
 * Node-av video encoder backend
 *
 * Re-exports from the existing node-av implementation for backward compatibility.
 */

export { NodeAvVideoEncoder } from '../../../node-av/NodeAvVideoEncoder.js';
